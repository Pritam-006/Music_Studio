<section
  id="about"
  class="relative overflow-hidden bg-[#0a0a0e] py-24 sm:py-32 lg:py-44"
>
  <!-- Top transition -->
  <div
    class="pointer-events-none absolute inset-x-0 top-0 h-40 bg-gradient-to-b from-[#08080b] via-[#0a0a0e]/80 to-transparent"
  ></div>

  <!-- Atmospheric glow -->
  <div
    class="pointer-events-none absolute -right-40 top-1/3 h-96 w-96 rounded-full bg-[#a855f7]/[0.07] blur-[150px]"
  ></div>

  <div class="relative mx-auto max-w-7xl px-5 sm:px-8 lg:px-10">

    <!-- Section heading -->
    <div
      class="reveal grid gap-10 lg:grid-cols-12 lg:items-end"
    >
      <div class="lg:col-span-7">
        <p
          class="mb-5 text-[10px] font-medium uppercase tracking-[0.3em] text-[#d8b4fe] sm:mb-6 sm:text-xs sm:tracking-[0.38em]"
        >
          About Doremi Fusion
        </p>

        <h2
          class="max-w-4xl text-[2.75rem] font-semibold leading-[0.94] tracking-[-0.045em] text-white sm:text-6xl lg:text-[5.8rem]"
        >
          Where music

          <span class="block text-white/60">
            becomes reality<span class="text-[#d946ef]">.</span>
          </span>
        </h2>
      </div>

      <div class="lg:col-span-4 lg:col-start-9">
        <p
          class="max-w-md text-[15px] leading-7 text-white/70 sm:text-base sm:leading-8"
        >
          A creative music studio in Kolkata where artists, musicians and
          ideas come together to create something worth listening to.
        </p>
      </div>
    </div>


    <!-- Main story -->
    <div
      class="mt-14 grid gap-10 sm:mt-20 sm:gap-12 lg:mt-28 lg:grid-cols-12 lg:items-center"
    >

      <!-- Image -->
      <div class="reveal reveal-delay-1 lg:col-span-7">
        <div
          class="about-image group relative overflow-hidden rounded-[3px] bg-[#121218]"
        >
          <div
            class="absolute inset-0 z-10 bg-gradient-to-t from-black/40 via-transparent to-transparent"
          ></div>

          <img
            src="/images/studio/p2.webp"
            alt="Inside Doremi Fusion Music Studio in Kolkata"
            loading="lazy"
            decoding="async"
            class="aspect-[4/3] h-full w-full object-cover transition-transform duration-700 ease-out group-hover:scale-[1.025]"
          />

          <!-- Image label -->
          <div
            class="absolute bottom-5 left-5 z-20 flex items-center gap-3 sm:bottom-6 sm:left-6"
          >
            <span class="h-px w-8 bg-[#c084fc]"></span>

            <span
              class="text-[10px] font-medium uppercase tracking-[0.22em] text-white/75 sm:tracking-[0.28em]"
            >
              Inside Doremi Fusion
            </span>
          </div>
        </div>
      </div>


      <!-- Text -->
      <div
        class="reveal reveal-delay-2 lg:col-span-4 lg:col-start-9"
      >
        <div class="border-l border-white/[0.14] pl-5 sm:pl-8">

          <p
            class="text-[17px] leading-8 text-white/85 sm:text-lg sm:leading-9"
          >
            Doremi Fusion is a full-service music studio built around
            creativity, quality and musical expression.
          </p>

          <p
            class="mt-6 text-[15px] leading-7 text-white/65 sm:mt-7 sm:text-base sm:leading-8"
          >
            From professional recording and music production to mixing,
            mastering and musical learning, we bring different parts of the
            creative process together under one roof.
          </p>

          <p
            class="mt-6 text-[15px] leading-7 text-white/65 sm:mt-7 sm:text-base sm:leading-8"
          >
            Whether you are recording your first song, developing a new
            arrangement or rehearsing with your band, Doremi Fusion is a
            space built to help turn musical ideas into reality.
          </p>

          <div class="mt-8 sm:mt-10">
            <a
              href="/#about"
              class="group inline-flex items-center gap-3 text-xs font-medium uppercase tracking-[0.16em] text-white/85 transition-colors duration-300 hover:text-white"
            >
              Discover our story

              <span
                class="text-[#e879f9] transition-transform duration-300 group-hover:translate-x-1"
              >
                →
              </span>
            </a>
          </div>

        </div>
      </div>

    </div>


    <!-- Bottom statement -->
    <div
      class="reveal reveal-delay-3 mt-16 border-t border-white/[0.1] pt-6 sm:mt-28 sm:flex sm:items-center sm:justify-between sm:pt-8"
    >
      <p
        class="text-[10px] font-medium uppercase tracking-[0.22em] text-white/45 sm:text-xs sm:tracking-[0.28em]"
      >
        Recording · Production · Mixing · Mastering
      </p>

      <p
        class="mt-4 text-[10px] font-medium uppercase tracking-[0.22em] text-white/40 sm:mt-0 sm:text-xs sm:tracking-[0.28em]"
      >
        Kolkata, India
      </p>
    </div>

  </div>
</section>

<style>
  .reveal {
    opacity: 0;
    transform: translateY(28px);
    transition:
      opacity 0.8s cubic-bezier(0.22, 1, 0.36, 1),
      transform 0.8s cubic-bezier(0.22, 1, 0.36, 1);
  }

  .reveal.is-visible {
    opacity: 1;
    transform: translateY(0);
  }

  .reveal-delay-1 {
    transition-delay: 0.08s;
  }

  .reveal-delay-2 {
    transition-delay: 0.16s;
  }

  .reveal-delay-3 {
    transition-delay: 0.24s;
  }

  @media (prefers-reduced-motion: reduce) {
    .reveal {
      opacity: 1;
      transform: none;
      transition: none;
    }
  }
</style>

<script>
  const elements = document.querySelectorAll(".reveal");

  const observer = new IntersectionObserver(
    (entries) => {
      entries.forEach((entry) => {
        if (entry.isIntersecting) {
          entry.target.classList.add("is-visible");
          observer.unobserve(entry.target);
        }
      });
    },
    {
      threshold: 0.12,
    },
  );

  elements.forEach((element) => observer.observe(element));
</script>